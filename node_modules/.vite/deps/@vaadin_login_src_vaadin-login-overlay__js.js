import {
  LoginFormMixin
} from "./chunk-WM6IFUR4.js";
import "./chunk-V7L4GO2Y.js";
import "./chunk-6QV6LNZP.js";
import "./chunk-MNTMWC4G.js";
import "./chunk-ETYEORRM.js";
import "./chunk-F6FMTQFS.js";
import {
  ifDefined
} from "./chunk-OSWHV746.js";
import {
  OverlayMixin,
  overlayStyles
} from "./chunk-PFI4EOEJ.js";
import "./chunk-4QGJQDIU.js";
import "./chunk-6KCLDTNY.js";
import "./chunk-C63QNJWN.js";
import "./chunk-2WIP7T36.js";
import "./chunk-ZZO67D7M.js";
import "./chunk-WJXDM45A.js";
import "./chunk-722MDOVI.js";
import "./chunk-ME7RDPE7.js";
import "./chunk-R7PLRAV7.js";
import "./chunk-SFISAMYO.js";
import {
  SlotChildObserveController
} from "./chunk-44TEYO45.js";
import "./chunk-XFKQJ73Z.js";
import "./chunk-X5EEA4XV.js";
import "./chunk-Z5LHTDPJ.js";
import "./chunk-VNVGWFKZ.js";
import "./chunk-WSKWP5BQ.js";
import "./chunk-T3UXCKHJ.js";
import "./chunk-472KGBTN.js";
import {
  ElementMixin
} from "./chunk-V6ISAKNA.js";
import "./chunk-OWHE56FA.js";
import "./chunk-YZIAODF7.js";
import "./chunk-GXEMU344.js";
import "./chunk-ZBUMLMW2.js";
import "./chunk-EIHDKHUQ.js";
import "./chunk-5W5MB6YY.js";
import "./chunk-M3HWWZVG.js";
import "./chunk-WT6HVPMS.js";
import "./chunk-ERLMGG3V.js";
import "./chunk-SVPOVVYN.js";
import "./chunk-5DESY7LS.js";
import {
  LumoInjectionMixin
} from "./chunk-ML4P6KOO.js";
import "./chunk-ULNR4UH6.js";
import {
  ThemableMixin
} from "./chunk-MKNWHZM7.js";
import "./chunk-TMRBPE5K.js";
import {
  DirMixin
} from "./chunk-56IVV4NW.js";
import {
  PolylitMixin
} from "./chunk-EQ5FNEVK.js";
import {
  defineCustomElement
} from "./chunk-D2WC5P34.js";
import {
  LitElement
} from "./chunk-4MR6LGSU.js";
import {
  html
} from "./chunk-ZCNTBWU5.js";
import {
  css
} from "./chunk-5IHOHWXL.js";
import "./chunk-MWWP2LZ3.js";
import "./chunk-5WRI5ZAA.js";

// node_modules/@vaadin/login/src/styles/vaadin-login-overlay-wrapper-base-styles.js
var loginOverlayWrapper = css`
  [part='overlay'] {
    background: var(
      --vaadin-login-overlay-background,
      var(--vaadin-overlay-background, var(--vaadin-background-color))
    );
    border: var(--vaadin-login-overlay-border-width, var(--vaadin-overlay-border-width, 1px)) solid
      var(--vaadin-login-overlay-border-color, var(--vaadin-overlay-border-color, var(--vaadin-border-color-secondary)));
    border-radius: var(--vaadin-login-overlay-border-radius, var(--vaadin-radius-l));
    box-shadow: var(--vaadin-login-overlay-shadow, var(--vaadin-overlay-shadow, 0 8px 24px -4px rgba(0, 0, 0, 0.3)));
  }

  [part='card'] {
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    max-width: 100%;
    overflow: hidden;
  }

  [part='brand'] {
    background: var(--vaadin-login-overlay-brand-background, var(--vaadin-background-container));
    display: flex;
    flex-direction: column;
    padding: var(--vaadin-login-overlay-brand-padding, var(--vaadin-padding-l));
  }

  ::slotted([slot='title']) {
    color: var(--vaadin-login-overlay-title-color, var(--vaadin-text-color));
    font-size: var(--vaadin-login-overlay-title-font-size, inherit);
    font-weight: var(--vaadin-login-overlay-title-font-weight, 600);
    line-height: var(--vaadin-login-overlay-title-line-height, inherit);
  }

  [part='description'] {
    color: var(--vaadin-login-overlay-description-color, var(--vaadin-text-color-secondary));
    font-size: var(--vaadin-login-overlay-description-font-size, inherit);
    font-weight: var(--vaadin-login-overlay-description-font-weight, inherit);
    line-height: var(--vaadin-login-overlay-description-line-height, inherit);
  }
`;
var loginOverlayWrapperStyles = [overlayStyles, loginOverlayWrapper];

// node_modules/@vaadin/login/src/vaadin-login-overlay-wrapper.js
var LoginOverlayWrapper = class extends OverlayMixin(DirMixin(ThemableMixin(PolylitMixin(LumoInjectionMixin(LitElement))))) {
  static get is() {
    return "vaadin-login-overlay-wrapper";
  }
  static get styles() {
    return loginOverlayWrapperStyles;
  }
  static get properties() {
    return {
      /**
       * Application description. Displayed under the title.
       */
      description: {
        type: String
      }
    };
  }
  /**
   * Override method from OverlayFocusMixin to use owner as focus trap root
   * @protected
   * @override
   */
  get _focusTrapRoot() {
    return this.owner;
  }
  /** @protected */
  render() {
    return html`
      <div id="backdrop" part="backdrop" ?hidden="${!this.withBackdrop}"></div>
      <div part="overlay" id="overlay">
        <div part="content" id="content">
          <section part="card">
            <div part="brand">
              <slot name="title"></slot>
              <div part="description">${this.description}</div>
            </div>
            <div part="form-wrapper">
              <slot></slot>
            </div>
          </section>
        </div>
      </div>
    `;
  }
};
defineCustomElement(LoginOverlayWrapper);

// node_modules/@vaadin/login/src/title-controller.js
var TitleController = class extends SlotChildObserveController {
  constructor(host) {
    super(host, "title", "div");
  }
  /**
   * Set title based on corresponding host property.
   *
   * @param {string} title
   */
  setTitle(title) {
    this.title = title;
    const titleNode = this.getSlotChild();
    if (!titleNode) {
      this.restoreDefaultNode();
    }
    if (this.node === this.defaultNode) {
      this.updateDefaultNode(this.node);
    }
  }
  /**
   * Set level based on corresponding host property.
   *
   * @param {string} level
   */
  setLevel(level) {
    this.level = level;
    if (this.node === this.defaultNode) {
      this.updateDefaultNode(this.node);
    }
  }
  /**
   * Override method inherited from `SlotController`
   * to customize heading on the default title node.
   *
   * @param {Node} node
   * @protected
   * @override
   */
  initNode(node) {
    if (node === this.defaultNode) {
      node.setAttribute("role", "heading");
    }
    this.host.setAttribute("aria-labelledby", node.id);
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to restore the default title element.
   *
   * @protected
   * @override
   */
  restoreDefaultNode() {
    const { title } = this;
    if (title && title.trim() !== "") {
      const node = this.attachDefaultNode();
      this.initNode(node);
    }
  }
  /**
   * Override method inherited from `SlotChildObserveController`
   * to update the default title element.
   *
   * @param {Node | undefined} node
   * @protected
   * @override
   */
  updateDefaultNode(node) {
    if (node) {
      node.textContent = this.title;
      node.setAttribute("aria-level", this.level);
    }
    super.updateDefaultNode(node);
  }
};

// node_modules/@vaadin/login/src/vaadin-login-overlay-mixin.js
var LoginOverlayMixin = (superClass) => class LoginOverlayMixin extends superClass {
  static get properties() {
    return {
      /**
       * Defines the application description
       * @type {string}
       */
      description: {
        type: String,
        value: "Application description",
        notify: true
      },
      /**
       * True if the overlay is currently displayed.
       * @type {boolean}
       */
      opened: {
        type: Boolean,
        value: false,
        reflectToAttribute: true,
        sync: true
      },
      /**
       * Defines the application title
       * @type {string}
       */
      title: {
        type: String,
        value: "App name"
      }
    };
  }
  /** @protected */
  firstUpdated() {
    super.firstUpdated();
    this.setAttribute("role", "dialog");
    this.setAttribute("aria-modal", "true");
    this.setAttribute("tabindex", "0");
    this.__titleController = new TitleController(this);
    this.addController(this.__titleController);
    this._overlayElement = this.$.overlay;
  }
  /** @protected */
  willUpdate(props) {
    super.willUpdate(props);
    if (props.has("__effectiveI18n") && this.__effectiveI18n.header) {
      this.title = this.__effectiveI18n.header.title;
      this.description = this.__effectiveI18n.header.description;
    }
  }
  /** @protected */
  updated(props) {
    super.updated(props);
    if (props.has("title") || props.has("__effectiveI18n")) {
      this.__titleController.setTitle(this.title);
    }
    if (props.has("headingLevel")) {
      this.__titleController.setLevel(this.headingLevel);
    }
    if (props.has("opened")) {
      this._openedChanged(this.opened);
    }
  }
  /** @protected */
  connectedCallback() {
    super.connectedCallback();
    if (this.__restoreOpened) {
      this.opened = true;
    }
  }
  /** @protected */
  disconnectedCallback() {
    super.disconnectedCallback();
    setTimeout(() => {
      if (!this.isConnected) {
        this.__restoreOpened = this.opened;
        this.opened = false;
      }
    });
  }
  /** @protected */
  _preventClosingLogin(e) {
    e.preventDefault();
  }
  /** @private */
  __handleOverlayClosed() {
    this.dispatchEvent(new CustomEvent("closed"));
  }
  /** @private */
  _openedChanged(opened, oldOpened) {
    if (oldOpened) {
      this._userNameField.value = "";
      this._passwordField.value = "";
      this.disabled = false;
    } else if (opened) {
      document.body.style.pointerEvents = this.$.overlay._previousDocumentPointerEvents;
    }
  }
  /**
   * Fired when the overlay is closed.
   *
   * @event closed
   */
};

// node_modules/@vaadin/login/src/vaadin-login-overlay.js
var LoginOverlay = class extends LoginFormMixin(LoginOverlayMixin(ElementMixin(ThemableMixin(PolylitMixin(LitElement))))) {
  static get is() {
    return "vaadin-login-overlay";
  }
  static get styles() {
    return css`
      :host([opened]),
      :host([opening]),
      :host([closing]) {
        display: block !important;
        position: absolute;
        outline: none;
      }

      :host,
      :host([hidden]) {
        display: none !important;
      }

      :host(:focus-visible) ::part(overlay) {
        outline: var(--vaadin-focus-ring-width) solid var(--vaadin-focus-ring-color);
      }
    `;
  }
  /** @protected */
  render() {
    return html`
      <vaadin-login-overlay-wrapper
        id="overlay"
        .owner="${this}"
        .opened="${this.opened}"
        .description="${this.description}"
        focus-trap
        with-backdrop
        theme="${ifDefined(this._theme)}"
        @vaadin-overlay-escape-press="${this._preventClosingLogin}"
        @vaadin-overlay-outside-click="${this._preventClosingLogin}"
        @vaadin-overlay-closed="${this.__handleOverlayClosed}"
        @opened-changed="${this._onOpenedChanged}"
        exportparts="backdrop, overlay, content, card, brand, description, form-wrapper"
      >
        <slot name="title" slot="title"></slot>
        <vaadin-login-form-wrapper
          id="form"
          .error="${this.error}"
          .i18n="${this.__effectiveI18n}"
          part="form"
          role="region"
          aria-labelledby="title"
          exportparts="error-message, error-message-title, error-message-description, footer"
        >
          <div id="title" slot="form-title" part="form-title" role="heading" aria-level="${this.headingLevel + 1}">
            ${this.__effectiveI18n.form.title}
          </div>
          <slot name="form" slot="form"></slot>
          <slot name="custom-form-area" slot="custom-form-area"></slot>
          <slot name="submit" slot="submit"></slot>
          <slot name="forgot-password" slot="forgot-password"></slot>
          <slot name="footer" slot="footer"></slot>
        </vaadin-login-form-wrapper>
      </vaadin-login-overlay-wrapper>
    `;
  }
  /** @private */
  _onOpenedChanged(event) {
    this.opened = event.detail.value;
  }
};
defineCustomElement(LoginOverlay);
export {
  LoginOverlay
};
//# sourceMappingURL=@vaadin_login_src_vaadin-login-overlay__js.js.map
